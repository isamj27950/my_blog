{% extends"base.html.twig" %}

{% block title %}
Accueil
{% endblock %}


{% block body %}
    <h1 class="text-indigo-700 text-3xl font-bold pb-4">Créer un nouvel article</h1>

    <div class="max-w-sm mx-auto">
        {{ form_start(showForm)}}
            {{form_widget(showForm)}}
            <div class="">
                <input type="submit" value="Envoyer" class="btn btn-secondary">
            </div>
        {{form_end(showForm)}}
    </div>

{% endblock %}